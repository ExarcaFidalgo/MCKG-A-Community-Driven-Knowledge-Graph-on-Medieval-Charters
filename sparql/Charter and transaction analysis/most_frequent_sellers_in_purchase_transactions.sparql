# Most Frequent Sellers in Purchase Transactions
PREFIX wd: <https://medievalcharterskg.wikibase.cloud/entity/>
PREFIX wdt: <https://medievalcharterskg.wikibase.cloud/prop/direct/>

SELECT ?person ?personLabel (COUNT(?acq) AS ?transactions)
WHERE {
  ?acq wdt:P3 wd:Q14 .
  ?acq wdt:P18 ?person .
  
   SERVICE wikibase:label {
    bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en" .
  }
}
GROUP BY ?person ?personLabel
ORDER BY DESC(?transactions)
